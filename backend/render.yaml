services:
  - type: web
    name: offbyone-backend
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/offbyone-0.0.1-SNAPSHOT.jar
    envVars:
      - key: SUPABASE_DB_PASSWORD
        sync: false
      - key: JWT_SECRET
        sync: false
