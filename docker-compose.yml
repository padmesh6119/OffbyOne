services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: offbyone
      POSTGRES_PASSWORD: offbyone
    ports:
      - "5432:5432"
    volumes:
      - ./backend/db/init:/docker-entrypoint-initdb.d
